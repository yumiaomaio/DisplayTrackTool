# Immersive Window Tool UI

This is the Vue 3 + Vite frontend for the Immersive Window Tool.
It is hosted within a WPF WebView2 control.

## Build

Run `npm run build` to build the single-file HTML and deploy it to the `ImmersiveWindow/WebUI` directory.
